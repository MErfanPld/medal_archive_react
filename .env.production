# Production — client demo / server deploy
# API base (no trailing slash)
NEXT_PUBLIC_API_URL=http://api.nasersolb.com

# Server-side proxy target (used if any same-origin rewrite is needed)
API_PROXY_TARGET=http://api.nasersolb.com

# Real API only (no mocks)
# NEXT_PUBLIC_USE_MOCK_AUTH=1
# NEXT_PUBLIC_USE_MOCK_DATA=1
